# ── EcoDial Carbon Footprint Awareness Console — Environment template ──
# Copy to `.env` for local configuration.

# Backend API Configuration
PORT=8000
HOST=0.0.0.0

# Allowed CORS origins (comma-separated list for backend main.py if customizable)
ALLOWED_ORIGINS=http://localhost:8080,http://127.0.0.1:8080
